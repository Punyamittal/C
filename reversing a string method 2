#include <stdio.h>
#include <string.h>
void main(){
    printf("enter the string:");
    char r[50];
    scanf("%s",r);
    int m=strlen(r);
    printf("the reversed string is:")
    for(int i=m-1;i>=0;i--){
        printf("%c",r[i]);
    }
}
